# Copy to .env and fill in. .env is gitignored — keep it that way.

# Alpaca PAPER trading (never live keys in here)
APCA_API_BASE_URL=https://paper-api.alpaca.markets
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=

# GitHub fine-grained PAT, scoped to this repo, Contents: read+write
GITHUB_TOKEN=

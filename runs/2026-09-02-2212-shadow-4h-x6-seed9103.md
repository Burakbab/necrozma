# 2026-09-02 ~21:47-22:12 UTC — 3-hourly check: a third seed, zero fold-gate clears (unlike seed 9102), and a new concentration-fix hypothesis still hard-fails

## Context

Item 2's unconstrained-search sub-thread has now run one generation on seed
9101 (06:46-07:15 UTC, hard-failed), and two generations on seed 9102
(19:00-19:27 UTC, generation 2 cleared the fold gate for the first time in
the sub-thread but failed the sealed holdout). Both prior entries left "more
generations/seeds" (option (i)) open as the only live, non-owner-decision-
gated thread under item 2 — every other AGENTS.md "Next steps" item is
currently blocked on a human/owner call (item 2's own accept-the-stack-vs-
redirect fork, item 4 has zero pending hard calls, item 5 needs sign-off,
item 6 needs a data-source pick before any code, per its own 15:46 UTC
design-pass entry); item 7 is feature-complete. This session picked up
option (i) again with a fresh seed.

Also checked before starting: `live_state.json`'s `updated` timestamp
(2026-09-02T00:22:38Z) matches today's already-handled 00:20 UTC daily tick
(`runs/2026-09-02-0020-daily-trading.md`) and today's 20:30 UTC daily
evaluation (`runs/2026-09-02-2030-daily-evaluation.md`) — nothing new to
trade this cycle. `python3 -m pytest -q` 338/338 baseline before touching
anything, confirmed clean. Repo note: this session's clone started with a
detached `HEAD` and a local `main` that had no common ancestor with
`origin/main` (a prior force-push had rewritten origin's history past this
container's stale local ref) — resolved with `git checkout main && git
reset --hard origin/main` before doing anything else; working tree was
already clean, no local work was at risk.

## What ran

```
python3 tools/shadow_4h_ramp_generation.py --recipe x6 --generations 2 --seed 9103
```

A third seed (9103, distinct from 9101/9102), 2 real `EvolutionRun.generation()`
calls against real 4h data, the unpatched `x6` seed genome. Read-only: this
tool never calls `save()`/`promote()` against the live account —
`live_state.json` untouched throughout, verified unmodified after the run
(same md5 before/after). Took ~868s (~14.5 min) for 2 generations, within
this thread's previously-noted range.

One process hiccup this session, unrelated to the code: the first attempt at
this run was killed (exit 137) by a mid-session worker restart, not by the
script itself. Re-ran clean per AGENTS.md item 9's guidance (backgrounded the
`python3` command directly via the tool's own background mode, no
`nohup ... &` wrapper) and it completed normally.

## Result

**Both generations hard-fail the real fold-aggregate gate on every top
candidate — no candidate this seed produced ever reached the sealed holdout
check.** Unlike seed 9102 (whose generation 2 cleared the fold gate once),
seed 9103's best candidates across both generations all hit
`state/lineage.jsonl`'s same rejection reason: `"challenger failed a hard
gate (too few trades, too short, or drawdown > 40%)"`.

Generation 1's top candidate is a hypothesis not previously named in this
thread's write-ups: `agents.risk_judge.genes.max_position_pct: <default> ->
0.175`, hypothesis text "circuit breaker tripped 5x — concentration too
high" (fold fitness 0.816 vs. seed champion's -2.531 hard-fail sentinel).
Capping position concentration is a genuinely different lever than every
prior tried angle (SCALE, `consv1` thresholds, `trailing_stop`, cold-start
ramp/cap/boost genes, regime-scale cuts, cash-floor increases) — but it
hard-fails the same gate the same way. Generation 2's 6 candidates (blind
search around the same seed champion, 22 cumulative proposals tried) stayed
at or below the champion's own -2.531 fitness; the best was -2.453, still a
hard-fail.

## Reading

**Third seed, third instance of the sub-thread's dominant pattern: 2 of 3
generations-with-a-positive-fold-fitness-leader (9101 gen 1, 9103 gen 1) hard
-fail the gate anyway, and only 1 of 4 generations run across all three
seeds so far (9102 gen 2) has ever cleared it — and that one then failed
holdout.** Nothing across 3 seeds / 5 generations total has cleared both
fold and holdout. This is more evidence for "near-misses, not real
solutions" from more search, consistent with every prior entry, not a
reversal of anything. The new `max_position_pct` hypothesis (concentration,
via the circuit breaker) is worth noting as a fourth distinct *kind* of
lever now tried and failed, alongside sizing/regime-scale, consult-threshold,
and cash-floor levers — narrowing further what "the search's own ideas"
actually look like when they hit this wall.

## What this does not change

- Item 2's owner-decision fork (accept the full `consv1 + trailing_stop +
  ramp` stack and move toward a real promotion attempt, or redirect effort)
  is untouched — still flagged for the next session/owner call, not decided
  here.
- No code changed this entry — pure use of already-committed, already-tested
  tooling (`tools/shadow_4h_ramp_generation.py`, unmodified). `python3 -m
  pytest -q` not re-run after (no code touched); baseline 338/338 confirmed
  before this entry's work started.
- `live_state.json` untouched, no protected file touched, no
  `AMENDMENTS.md` row needed (no constitution change). Genome still v3 (1d)
  live, untouched.

## Next

Sample is now 3 seeds / 5 generations across this sub-thread, with exactly
one fold-gate clear (which then failed holdout). If another session picks up
option (i) again, that ratio (1/5 fold-clears, 0/5 holdout-clears) is worth
weighing against whether more seeds are still the highest-value use of a
slot versus treating option (i) as similarly exhausted to the single-lever
alternatives closed at 12:47-13:09 UTC — that judgment call is left open
here, not made.

# 2026-09-03 ~03:46-04:16 UTC — 3-hourly check: a fifth seed, a third fold-gate clear (same recurring candidate), still 0/9 at holdout

## Context

Item 2's unconstrained-search sub-thread stood at 4 seeds / 7 generations
going into this cycle (9101: 1 gen hard-fail; 9102: 2 gens, gen 2 cleared the
fold gate once then failed holdout; 9103: 2 gens, both hard-fail; 9104: 2
gens, gen 1 cleared the fold gate a second time then failed holdout). Every
other "Next steps" item is still blocked on a human/owner call (item 2's own
accept-the-stack-vs-redirect fork, item 5 needs sign-off, item 6 needs a
data-source pick) or effectively closed (item 3, item 7, item 8). This
session picked up option (i) again with a fifth fresh seed.

Also checked before starting: `live_state.json`'s `updated` timestamp
(2026-09-03T00:22:19Z) matches today's already-handled 00:20 UTC daily tick
(`runs/2026-09-03-0020-daily-trading.md`, tick 20, held/no trade) — nothing
new to trade this cycle. `python3 -m pytest -q` 338/338 baseline confirmed
before touching anything (120.73s).

Repo note, same as the prior sessions: this session's clone started with a
detached `HEAD` and a local `main` sharing no common ancestor with
`origin/main` (stale ref from before an earlier force-push rewrote origin's
history). Resolved with `git reset --hard origin/main` (per this file's own
"Get onto the branch, then pull" step) before starting; working tree was
already clean, nothing lost.

## What ran

```
python3 tools/shadow_4h_ramp_generation.py --recipe x6 --generations 2 --seed 9105
```

A fifth seed (9105, distinct from 9101-9104), 2 real `EvolutionRun.generation()`
calls against real 4h data, the unpatched `x6` seed genome. Read-only: this
tool never calls `save()`/`promote()` against the live account —
`live_state.json` md5 confirmed unchanged after the run (`b8602875...`, same
before and after). Took 934s (~15.6 min) for 2 generations, consistent with
this thread's previous timings.

## Result

**Generation 1's candidate #3 cleared the real fold-aggregate hard gate — the
third clear across the sub-thread — and again failed the sealed holdout. The
winning-at-fold proposal is the exact same mutation as seed 9104's
fold-clear, `agents.consult_moderate.genes.enabled: True -> False`
(`remove_agent`), with the identical fold fitness (0.0443) to four decimal
places.** `state/lineage.jsonl` confirms it passed `dd_corrected_stats()`'s
gate cleanly (rejection reason recorded only at the holdout stage, no "failed
a hard gate" entry) and lost at holdout: `failed sealed holdout: -0.724 did
not clear champion -0.222 + margin 2.355 (1 cumulative draws against this
holdout)`. Generation 1's other two top candidates (`regime_scale.bull: 1.0
-> 1.5`, fold fitness 0.496; a `min_rank_mom`+`unanimous_bonus` tune, fold
fitness 0.233) both hard-failed the gate ("too few trades, too short, or
drawdown > 40%") despite higher fold fitness than the holdout-tested one —
same fold-fitness-rank-vs-gate-clearing mismatch as every prior entry.

Generation 2 (6 candidates, 22 cumulative proposals against this seed) found
nothing better: all three inspected top candidates hard-failed the gate, best
fold fitness -2.387 (well below this generation's own top raw fitness of
0.513, which never reached `dd_corrected_stats()` at all because only the
top-3 by fold fitness are checked and #1 hard-failed first — no holdout draw
recorded this generation).

**Notable methodological observation, not acted on:** the `consult_moderate`
disable candidate reappearing with an *identical* fold fitness (0.0443) in
both seed 9104 and seed 9105's generation-1 top-8 suggests this specific
`remove_agent` proposal is a structural/deterministic candidate in the
researcher's blind-mutation set (one of a handful of fixed "disable each
agent" probes tried every generation), not something drawn from the seeded
RNG stream the way `tune` mutations are. The champion's own holdout fitness
moved slightly between the two runs (-0.265 -> -0.222), consistent with
as-of drift from ~3 hours of new bars, not a re-run artifact. Nothing here
changes the reading below; flagging only so a future session doesn't mistake
this recurrence for a coincidence worth re-investigating.

## Reading

**Updated tally across the sub-thread: 5 seeds / 9 generations total, 3
fold-gate clears (9102 gen 2, 9104 gen 1, 9105 gen 1), 0 of 9 ever clearing
both fold *and* holdout.** All three fold-clears are de-risking moves (a cash
floor, and now twice the same consult-agent removal) that duck fold 1's
drawdown window without adding real edge elsewhere — the third data point is
the same underlying mutation as the second, so it strengthens the "this
specific move ducks the fold, still fails holdout" finding more than it adds
a genuinely independent data point against "unconstrained search never finds
real edge here."

**This session's read, unchanged in direction from the 01:11 UTC entry and
now on a fifth independent seed: option (i)'s marginal information has
thinned further, not reversed.** Five seeds and nine generations without a
single holdout-clear, and the one fold-clearing move that recurs does so for
a structural reason (a fixed candidate in the mutation set) rather than
because a fresh seed found something new — this makes "run more seeds" even
less likely to surface a materially different result than the 01:11 UTC
entry already argued. Not closing option (i) outright here — that is
explicitly the next session/owner's call per the existing fork, not a
unilateral call from a single 3-hourly cycle — but this session's
recommendation is now more concrete than "the bar has shifted": **the next
session/owner should treat option (i) as exhausted for the `x6` recipe with
`n_blind=6` and make the item 2 accept-the-stack-vs-redirect decision
explicitly**, rather than defaulting to a sixth seed.

## What this does not change

- Item 2's owner-decision fork (accept the full `consv1 + trailing_stop +
  ramp` stack and move toward a real promotion attempt, or redirect effort)
  is untouched — still flagged for the next session/owner call, not decided
  here.
- No code changed this entry — pure use of already-committed, already-tested
  tooling (`tools/shadow_4h_ramp_generation.py`, unmodified). `python3 -m
  pytest -q` not re-run after (no code touched); baseline 338/338 confirmed
  before this entry's work started.
- `live_state.json` untouched (md5 identical before/after), no protected
  file touched, no `AMENDMENTS.md` row needed (no constitution change).
  Genome still v3 (1d) live, untouched.

## Next

Sample is now 5 seeds / 9 generations across this sub-thread: 3 fold-gate
clears (one mutation recurring twice), 0 holdout-clears. This session's
recommendation, sharper than the 01:11 UTC entry's: stop running additional
seeds against the bare `x6` recipe and have the next session/owner make the
item 2 accept-vs-redirect call explicitly instead.

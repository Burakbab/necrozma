# Evolve batch against live v3 champion — 2026-09-06 ~12:47-13:2x UTC

3-hourly self-improvement check. 15 more real `evolve` generations against the
live v3 (1d) champion. No promotion.

## Freshness checks before running

- `live_state.json`'s `updated` timestamp: `2026-09-06T10:25:27+00:00` — today's
  daily bar already handled at 00:20 UTC (tick 23, confirmed via
  `runs/2026-09-06-0020-daily-trading.md`); no live trading this cycle.
- `review-hard-calls`: 0 pending (1 reviewed so far, unchanged).
- `holdout-pressure`: same fold-clears-then-loses-holdout shape as every recent
  run, nothing new.
- `live-benchmark`: still 22 1d bars, excess -11.07% — unchanged, nowhere near
  item 0's 60-bar revisit trigger.
- Items 2/5/6 (`AGENTS.md` "Owner decisions pending") still blocked with no
  new owner input since this morning's daily discussion.
- `python3 -m pytest -q`: 366/366 baseline, confirmed before starting (no code
  changed this entry).

Concluded, same as every recent cycle: nothing else was unblocked or
differently-shaped enough to prefer over continuing real cumulative search
against the actual live champion.

## Run

`python3 evotrader_bundle.py evolve 15` (backgrounded directly, no `nohup`,
per item 9's operational note).

Cumulative candidates tried against v3 rose 2881 → 3090 (14 new proposals per
generation over 15 generations — the fixed post-boldness-fix batch shape),
boldness/stagnation counter 204 → 219. Champion fitness held flat at 1.215
across all 15 generations; no candidate cleared the full gate.

Best-of-generation raw fold-fitness numbers this batch: 1.348, 1.330, 2.078,
1.565, 1.313, 1.220, 1.247, 2.732, 1.268, 0.946, 1.396, 1.247, 2.009, 1.420,
1.549. That's 14/15 generations where the best raw candidate beat the
champion's own 1.215, before losing at the fold-aggregate/holdout gate —
continuing to track the ratio the 2026-09-06 06:55 UTC boldness-saturation
fix entry flagged as worth watching: pre-fix 12/25 (48%), first post-fix
batch 17/25 (68%), second post-fix batch 14/20 (70%), this batch 14/15 (93%).
Combined post-fix rate is now 45/60 (75%) vs. the pre-fix 12/25 (48%) —
still consistent with the fix restoring more local competitive search, and
the trend continues to point the same direction each batch, though this
remains an accumulating observation rather than a formally tested claim.

## Verification before commit

- `git show HEAD:live_state.json` vs. working tree: direct key-by-key
  comparison showed only `lineage`, `researcher_memory`, `updated` changed —
  `genome`, `broker`, `journal`, `hard_call_reviews`, `started`, `ticks` all
  byte-identical.
- `python3 evotrader_bundle.py summary`: constitution verified
  `8b74865634b1db07` (unchanged), genome version still 3, ticks still 23 (no
  duplicate trade).
- `python3 -m pytest -q`: 366/366, unchanged — no code touched this entry.
- Dashboard rebuilt (`EVO_STATE=... python3 evotrader_dashboard.py`).

Genome still v3 (1d) live, untouched.

# Evolve batch against live v3 champion — 2026-09-07 ~09:47-10:31 UTC

## Context

3-hourly check. Daily bar already handled at 00:20 UTC (`runs/2026-09-07-0020-daily-trading.md`,
`live_state.json`'s `updated` was `2026-09-07T07:08:47+00:00` before this run) — no live
trading this cycle. Freshness checks before starting:

- `review-hard-calls`: 0 pending (1 reviewed total, unchanged).
- Items 2/5/6 in `AGENTS.md`'s "Owner decisions pending": still no new owner input since
  yesterday's daily discussion.
- `python3 -m pytest -q`: 366/366 passing (baseline, confirmed before any change).

Nothing else was differently-shaped enough to prefer over continuing real cumulative search
against the live champion.

## What ran

`python3 evotrader_bundle.py evolve 15` — 15 more real generations against the live v3 (1d)
champion. Cumulative candidates tried against v3 rose 4343 → 4551 (14 proposals/generation for
13 of 15 generations, 13 for the other 2), boldness/stagnation counter 309 → 324.

Champion fitness held flat at **1.422** across all 15 generations; every new candidate lost to
it — same local optimum as the last several batches. No promotion.

Raw best-of-generation fold-fitness beat the champion's own 1.422 in **9/15 generations (60%)**
this batch: gens 3, 5, 6, 7, 8, 9, 10, 11, 13. This continues the pattern the last two batches'
"Current state" entries flagged as worth watching (53%, then 47%, both down from the post-fix
peak of 76%/75%) — this batch's 60% sits between those two readings and the higher post-fix
rates, not a continuation of a clean decline. Read as noise around an unsettled rate, consistent
with the last three batches (53%, 47%, 60%) all landing well below the post-fix peak but not
converging on a single number yet.

## Verification before commit

- `python3 -m pytest -q`: 366/366 (baseline, unchanged — no code touched this cycle).
- Direct key-by-key diff of `live_state.json` (old vs new, via `git show HEAD:live_state.json`):
  only `lineage`, `researcher_memory`, `updated` changed. `genome`, `broker`, `journal`,
  `hard_call_reviews`, `started`, `ticks` all byte-identical.
- Constitution verified `8b74865634b1db07` unchanged (printed by every bundle command).
- `tools/edit_bundle_module.py verify`: round-trip verified, bundle unchanged.
- Genome still v3 (1d) live, untouched. `ticks` still 24 (no live trade this cycle).

Dashboard rebuilt (`EVO_STATE=... python3 evotrader_dashboard.py`) to pick up the new lineage
entries.

# Evolve batch against live v3 — 2026-09-07 21:49-22:17 UTC

## Scope

Routine 3-hourly self-improvement cycle. Daily bar already handled (tick 24
at 00:20 UTC, confirmed via `live_state.json`'s `updated` timestamp before
this cycle and the 20:30 UTC daily-evaluation note) — no live trading this
cycle. Ran 15 more real `evolve` generations against the live v3 (1d)
champion.

## Freshness checks before running

- `review-hard-calls`: 0 pending (1 reviewed so far, unchanged).
- `holdout-pressure`: same fold-clears-then-loses-holdout shape as every
  prior run — 19 individual sealed-holdout draws lost, 0 accepted. No new
  information.
- `live-benchmark`: unchanged, 23 1d bars, -12.60% excess vs. equal-weight
  buy-and-hold, still far from the 60-bar revisit trigger.
- Owner-decision items 2/5/6 (AGENTS.md "Owner decisions pending") still
  blocked, no new input since the 2026-09-06 weekend all-hands.

## Batch result

Cumulative candidates tried against v3 rose **4760 → 4969**, boldness/
stagnation counter **339 → 354**. Champion fitness held flat at 1.422 across
all 15 generations; every new candidate lost to it (`[evolve] champion
held`). Raw best-of-generation fold-fitness beat the champion's own 1.422 in
**9/15 generations** this batch (60%): gens 1, 2, 6, 9, 10, 11, 13, 14, 15
beat it (gen 4 tied exactly at 1.422, not counted as a beat); gens 3, 5, 7,
8, 12 came in below. Consistent with the last several batches' range (47%
-73%), read as noise around an unsettled rate, not a trend.

## Process note

Started the batch with a shell-function-plus-trailing-`&` pattern under
`run_in_background: true` — this reproduces exactly the footgun AGENTS.md
item 9 already warns about (`nohup ... &`-style detachment): the wrapper
tool call reported "exited with code 0" almost immediately while the real
`python3 evotrader_bundle.py evolve 15` process (confirmed via `ps aux`)
kept running orphaned for the full ~28 minutes. No harm this time — caught
immediately, verified the real PID was still alive with `kill -0` in a poll
loop instead of trusting the wrapper's completion signal, per the existing
process note. Restating the existing rule for the next session since one
attempt at circumventing it (via a different shell-quoting path) still
recreated it: **background the `python3 evotrader_bundle.py evolve N`
command directly, with nothing wrapping it in its own subshell-and-`&`**.

## Verification before commit

- `python3 -m pytest -q` 366/366, run *before* starting `evolve` as the
  baseline (not concurrently, per the 2026-09-07 13:34 UTC race-fix's own
  process note) — unchanged, no code touched this cycle.
- Direct top-level key diff of `live_state.json` (before vs. after, via
  `git show HEAD:live_state.json`): only `lineage`/`researcher_memory`/
  `updated` changed; `genome`, `broker`, `journal` byte-identical.
- Constitution verified `8b74865634b1db07`, unchanged.
- `tools/edit_bundle_module.py verify` clean (round-trip unchanged).
- `tools/edit_bundle_module.py sync --check` clean (bundle already matches
  real files, no drift).

Genome still v3 (1d) live, untouched.

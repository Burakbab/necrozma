# Evolve batch: 15 generations against live v3, no promotion

3-hourly check, ~21:46-22:18 UTC.

## Freshness checks before running

- `live_state.json`'s `updated` timestamp was `2026-09-08T19:14:04+00:00`
  (last 3-hourly evolve batch), and today's daily bar (tick 25) was already
  handled at 00:20 UTC and confirmed clean again by the 20:30 UTC daily
  evaluation (`runs/2026-09-08-2030-daily-evaluation.md`) — no new bar to
  trade this cycle, so `tick` was not run.
- Repo was in detached HEAD with a shallow clone reporting local `main` and
  `origin/main` as diverged with no merge-base — the documented shallow-fetch
  artifact (`tools/git_sync.py`, AGENTS.md run-protocol step 2). Ran
  `python3 tools/git_sync.py`, which unshallowed, found a real merge-base,
  and fast-forwarded cleanly (`4f15e68..f34f5fc`) — nothing discarded.
- `review-hard-calls`: 0 pending (1 reviewed so far, unchanged).
- `holdout-pressure`: same fold-clears-then-loses-holdout shape as always
  (13 individual holdout draws, all lost, 0 accepted) — nothing new.
- Items 2/5/6 still blocked with no new owner input; item 4 (redirect target
  from item 2) still has 0 pending hard-call reviews to act on, so this
  cycle ran the standing evolve batch rather than a new engineering slice.

## Batch result

Champion fitness held flat at 1.590 across all 15 generations; every new
candidate lost to it. Cumulative candidates tried against v3 rose 6429 →
6637, boldness/stagnation counter 459 → 474. Raw best-of-generation
fold-fitness strictly beat the champion's own 1.590 in 7/15 generations
(47%: gens 2, 6, 8, 11, 12, 14, 15), with 3 more tying exactly (gens 3, 5,
13) — in the same noisy range as recent batches, no trend.

## Verification before commit

- `python3 -m pytest -q`: 384/384 (baseline, run strictly before `evolve`,
  unchanged — no code touched this cycle).
- Direct key-by-key diff of `live_state.json`: only `broker`, `lineage`,
  `researcher_memory`, `updated` differ from the pre-evolve commit.
  `broker.cash`/`positions`/`nav_history` all byte-identical; the only
  `broker` change is `borrow_bps_per_bar` going from absent (`None`, i.e.
  not yet in the pre-existing serialized state) to its default `0.0` — the
  short-selling schema field from the previous commit getting written out
  for the first time on this run's save, not a trade. Genome byte-identical
  (still v3).
- Constitution verified `726dfa4bac85891a` unchanged (matches the
  short-selling amendment's reseal from the previous commit).
- `tools/edit_bundle_module.py verify`: clean (bundle round-trip unchanged).
- `tools/edit_bundle_module.py sync --check`: clean (real files match
  bundle).

Genome still v3 (1d) live, untouched. Dashboard rebuilt
(`evotrader_dashboard.py`) to reflect the updated lineage counters.

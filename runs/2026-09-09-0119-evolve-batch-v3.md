# Evolve batch: 15 more generations against live v3 — 2026-09-09 00:46-01:19 UTC

- Repo sync: cloud clone started in detached HEAD as usual (`git checkout main`
  + `python3 tools/git_sync.py`); reported the usual shallow-clone false
  divergence ("diverged, 50 and 50 different commits each") and fast-forwarded
  cleanly onto `origin/main` — nothing discarded, nothing lost.
- `pip3 install -r requirements.txt -q` — clean.
- No live trading this cycle: today's daily bar (tick 26, 2026-09-08 bar) was
  already handled at 00:20 UTC (`runs/2026-09-09-0020-daily-trading.md`),
  confirmed via `live_state.json`'s `updated` timestamp before starting.
  `review-hard-calls` still 0 pending (item 4 has no real case to act on).
- `python3 -m pytest -q` baseline **384/384**, run strictly before `evolve`,
  no code touched this cycle.
- 15 more real `evolve` generations against the live v3 (1d) champion, no
  promotion — cumulative candidates tried against v3 rose 6637 → 6847,
  boldness/stagnation counter 475 → 489. Champion fitness held flat at 0.977
  across all 15 generations; every new candidate lost to it on the
  fold-aggregate gate. Raw best-of-generation fold-fitness beat the
  champion's own 0.977 in **15/15 generations this batch (100%)** — every
  single generation this cycle produced a raw winner that still failed to
  clear the full acceptance gate (holdout / consistency / drawdown), the
  highest beat-rate recorded in this file's recent tracking (prior batches
  ranged 27-73%). Read as more evidence for the standing pattern
  (`holdout-pressure`) that real fold-superior challengers exist regularly
  and lose at the sealed holdout, not as a new finding — flagging the number
  here since it's an outlier worth a future session checking the actual
  candidate genomes if the rate stays this high.
- **Process near-miss, no harm**: recreated the item-9 nohup/`&` footgun via
  a *new* path — passed `run_in_background: true` to the Bash tool AND put a
  trailing `&` in the command string itself. The tool's own background job
  finished almost instantly (the two-line wrapper: launch-and-echo-PID) and
  reported "completed", while the real `python3 evotrader_bundle.py evolve
  15` process (PID 2521) kept running detached for ~29 more minutes. Caught
  immediately by checking `live_state.json`'s `updated` timestamp (unchanged)
  and `kill -0 2521` (still alive) before touching any state; polled the real
  PID to actual exit with a background loop before doing anything else. No
  state was read, diffed, or committed until the real process was confirmed
  dead. Distinct trigger from the five prior recurrences logged under item 9
  (this one is "the tool's own background flag *plus* a shell `&`", not a
  `nohup ...  &` string) — worth adding to item 9's pointer if it recurs
  again under yet another shape.
- Verified before commit: `python3 -m pytest -q` 384/384 baseline (above, run
  before `evolve`, unchanged — no code touched), direct key-by-key diff of
  `live_state.json` (committed HEAD vs. working tree) showed only
  `lineage`/`researcher_memory`/`updated` changed (`genome`, `broker`,
  `journal`, `ticks`, `hard_call_reviews`, `started` byte-identical),
  constitution verified `726dfa4bac85891a` unchanged,
  `tools/edit_bundle_module.py verify`/`sync --check` both clean. Genome
  still v3 (1d) live, untouched — no README.md `## Status` update needed.
- Dashboard rebuilt: `EVO_STATE="$(pwd)/live_state.json" python3
  evotrader_dashboard.py` → `index.html` (20.5 KB).

# 3-hourly check, 2026-09-10 ~03:47-04:32 UTC

Repo started in detached HEAD with local `main` badly stale (60+ commits
behind `origin/main`, no shared tip visible at shallow depth) — same
documented shallow-clone false-divergence AGENTS.md's run protocol
describes. Resolved with `git checkout main && git reset --hard origin/main`
on a clean tree (no local commits to lose); `tools/git_sync.py` was not
reached for first, same recurring habit lapse prior entries have flagged.

No live trading this cycle: tick 27 already handled by the 00:20 UTC daily
run (confirmed via `live_state.json`'s `updated` timestamp and the prior
`2026-09-10-0113-evolve-batch-v3-background-runner-first-use.md` note before
starting).

Freshness checks before running: `review-hard-calls` still 0 pending (1
reviewed so far, unchanged); `holdout-pressure` unchanged in shape (235
draws, all lost, margin ~6.6 going in). Items 2/5/6 still not a scheduled
session's call (item 2 parked, item 5 shipped but un-scoped for wiring, item
6 blocked on a data-source decision); item 4 still has 0 pending hard-call
reviews to act on; item 7 explicitly optional/last. No unstarted, unblocked
roadmap item found, so the cycle ran the standing `evolve` batch.

**15 more real `evolve` generations against the live v3 (1d) champion, no
promotion** — cumulative candidates tried against v3 rose 8305 → 8512
(cumulative from log: 8319 → 8512, i.e. +193 across generations 2-15 plus
the first 14 landing at 8319), boldness/stagnation counter 594 → 609. Ran via
`tools/background_runner.py` (`start` + backgrounded `wait`, one mechanism
each): `start` returned instantly with the real child PID, `wait` blocked
the full ~36-minute run (2180s) and returned the real exit code (0) from the
child's own status-file write. No nohup/`&`, log captured all 15
generations' output from the first byte.

Champion's fold-aggregate fitness held flat at 1.469 across all 15
generations; every new candidate lost to it on the full acceptance gate.
Raw best-of-generation fold-fitness beat the champion's own 1.469 in 12/15
generations, tied exactly in 1 (generation 2), lost in 2 (generations 3 and
7, 1.459 and 1.167) — an ordinary batch, consistent with the standing
fold-clears-then-loses-holdout pattern, no new finding.
`holdout-pressure` confirms: 235 → 237 real draws, every one still lost,
margin ~6.61-6.61 (unchanged shape).

**Housekeeping**: the evolve-batch log this cycle was accidentally written
under `runs/` (`runs/evolve_batch_0349.log`) instead of `/tmp`, which would
have left a stray untracked file in the repo on every future batch run.
Fixed by adding `runs/*.log` to `.gitignore` (committed separately,
`27f6f91`, before this note) — `runs/` stays dated-notes-only going forward
regardless of where a session points `background_runner.py --log`.

Verified before commit: `python3 -m pytest -q` 390/390 both before
(baseline, run strictly before `evolve`) and after; direct key-by-key
Python-equality diff of `live_state.json` against `HEAD` showed only
`lineage`/`researcher_memory`/`updated` changed (`genome`, `broker`,
`journal`, `hard_call_reviews` byte-identical); constitution verified
`726dfa4bac85891a` unchanged (`evotrader_bundle.py summary` reprints it);
`tools/edit_bundle_module.py verify`/`sync --check` both clean. Genome still
v3 (1d) live, untouched.

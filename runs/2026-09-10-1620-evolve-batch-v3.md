# Evolve batch v3, 2026-09-10 ~15:50-16:20 UTC

15 more real `evolve` generations against the live v3 (1d) champion, run via
`tools/background_runner.py` (`start` + backgrounded `wait`). No promotion.

## Pre-run freshness checks

- `live_state.json`'s `updated` was `2026-09-10T13:29:44+00:00`, `ticks: 27` —
  today's daily bar already handled at 00:20 UTC (`runs/2026-09-10-0020-daily-trading.md`).
  No live trading this cycle.
- `review-hard-calls`: 0 pending, 1 reviewed so far — unchanged.
- Items 2/5/6 in AGENTS.md's "Owner decisions pending" / roadmap still not a
  scheduled session's call. No other unstarted, unblocked roadmap item found,
  so the cycle ran the standing evolve batch.
- Baseline `python3 -m pytest -q`: 390/390 passed, run before `evolve`.

## Batch results

- Champion's fold-aggregate fitness held flat at 1.469 across all 15
  generations (961 trades, win 39%, stops 1%, halts 3 — unchanged).
- Cumulative candidates tried against v3: 9136 → 9344 (208 new candidates:
  14/generation × 15, minus a small rounding note in the log's own running
  counter). Boldness/stagnation counter: 654 → 669.
- Raw best-of-generation fold-fitness vs. the champion's own 1.469: beat in
  12/15 generations, tied in 1 (gen 9, exactly 1.469), lost in 2 (gen 2 at
  1.365, gen 8 at 1.113) — an ordinary batch, nothing unusual.
- `holdout-pressure`: 242 → 245 real draws, every one still lost, margin
  ~6.61-6.63 — unchanged in shape from the prior check.

## Verification before commit

- `python3 -m pytest -q`: 390/390 both before (baseline) and after `evolve`.
- Direct key-by-key diff of `live_state.json` (old vs. new via `git show
  HEAD:live_state.json`): only `lineage`/`researcher_memory`/`updated`
  changed. `genome`/`broker`/`journal`/`hard_call_reviews` byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged (`evotrader_bundle.py
  summary`).
- `tools/edit_bundle_module.py verify` — round-trip verified, bundle
  unchanged. `sync --check` — bundle already matches real files.
- Dashboard rebuilt (`index.html`), since `lineage`/`researcher_memory`
  counts feed its genome panel.

Genome still v3 (1d) live, untouched.

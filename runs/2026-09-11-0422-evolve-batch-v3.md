# 3-hourly check: evolve batch against live v3

**Time:** 2026-09-11 ~03:48-04:22 UTC

## Pre-flight

- Repo started in detached HEAD with local `main` stale at `4f15e68`
  (2026-09-04). Fixed with `git checkout -B main origin/main` after
  confirming the detached HEAD tip already matched `origin/main` (`5bfa8d4`)
  byte-for-byte — nothing local lost.
- `live_state.json` `updated`: 2026-09-11T01:15:20+00:00, `ticks: 28`.
  Today's daily bar already handled at 00:20 UTC
  (`runs/2026-09-11-0020-daily-trading.md`) and this same 3-hourly slot's
  own earlier standing evolve batch already ran at ~01:18 UTC
  (`runs/2026-09-11-0118-evolve-batch-v3.md`). No live trading this cycle.
- `review-hard-calls`: still 0 pending (1 reviewed so far, unchanged).
- Items 2/5/6 still not a scheduled session's call; item 4 still blocked on
  a real hard-call flag (none pending); item 7 feature-complete; item 8
  closed for v3; item 9's mechanical fix continues to work as designed. No
  other unstarted/unblocked roadmap item found, so this cycle ran the
  standing 15-generation `evolve` batch via `tools/background_runner.py`
  (`start` + backgrounded `wait`).

## Result

15 more real `evolve` generations against the live v3 (1d) champion, **no
promotion**. Champion's fold-aggregate fitness held flat at 1.656 across all
15 generations (963 trades, win 39%, stops 1%, halts 3 — unchanged).
Cumulative real candidates tried against v3 rose 9802 → **10009** (crossed
10,000), boldness/stagnation counter 703 → 717.

Raw best-of-generation fold-fitness vs. the champion's own 1.656:
win in 6/15 generations (1.787, 1.879, 1.759, 1.816, 1.755, 1.718), tied in
1 (1.656), lost in 8 — an ordinary batch, nothing anomalous.

`holdout-pressure`: draw count unchanged at 249 (every one still lost,
margin unchanged in shape ~6.6) — consistent with no candidate clearing the
fold-aggregate gate this batch, so no new sealed-holdout draws were spent.

## Verification before commit

- `python3 -m pytest -q`: 390/390 both before (baseline) and after `evolve`.
- Direct top-level key diff of `live_state.json`: only
  `lineage`/`researcher_memory`/`updated` changed; `genome`, `broker`,
  `journal`, `hard_call_reviews`, `ticks`, `started` byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged (`evotrader_bundle.py
  summary`).
- `tools/edit_bundle_module.py verify` and `sync --check` both clean.
- Dashboard rebuilt (`index.html`) since `lineage`/`researcher_memory`
  counts feed its genome panel.

Genome still v3 (1d) live, untouched.

# 3-hourly check: evolve batch v3 (gen 11257->11465), no promotion

~03:50-04:17 UTC.

## Pre-flight

- Repo started in detached HEAD, local `main` 4 commits behind `origin/main`
  (`853dfa2`). `git checkout main && git merge --ff-only origin/main` applied
  cleanly — a real fast-forward (detached HEAD tip already matched local
  `main`'s history), nothing local lost.
- `pip3 install -r requirements.txt -q` (bare sandbox).
- No live trading this cycle: tick 29 already handled at 00:20 UTC, confirmed
  via `live_state.json`'s `updated` timestamp (`2026-09-12T01:26:21+00:00`,
  from the prior 3-hourly check's evolve batch) and
  `runs/2026-09-12-0020-daily-trading.md`.
- Freshness checks before running: `review-hard-calls` 0 pending (1 reviewed
  so far, unchanged) — item 4 (LLM-backed consults) still blocked on a real
  hard-call flag. Items 2/5/6 still not a scheduled session's call. Item 7
  feature-complete. `holdout-pressure` at 255 draws pre-batch, every one
  still lost, margin unchanged in shape (~6.65).

## Evolve batch

Ran the standing 15-generation batch via `tools/background_runner.py`
(`start` + backgrounded `wait`; ~26.7-minute real run, exit code 0, no
truncation — full output captured to a log file from the first byte).

Champion's fold-aggregate fitness held flat at 1.508 across all 15
generations (964 trades, win 39%, stops 1%, halts 3 — unchanged). Cumulative
candidates tried against v3 rose 11257 → 11465, boldness/stagnation counter
807 → 822.

Raw best-of-generation fold-fitness vs. the champion's own 1.508: beat it in
7/15 generations (1.718, 1.723, 1.550, 1.950, 2.100, 1.753, 2.190), tied in 2
(gen 1, gen 14), lost in 6 — a livelier batch than several recent ones, still
no promotion (fold-aggregate clear alone isn't sufficient; nothing reached a
sealed-holdout win this batch either). `holdout-pressure` draw count rose
255 → 257 (both new draws still lost, margin unchanged in shape ~6.66).

## Verification

- `python3 -m pytest -q`: 391/391 baseline (before `evolve`).
- Post-batch full suite run showed 2 failures
  (`test_run_from_files_matches_bundle_output[holdout-pressure]` and
  `[holdout-margin-audit]`, both at the `after == before` byte-identity
  assertion on the real `live_state.json`). Investigated before treating
  this as a regression: re-ran just those two tests in isolation — both
  passed; re-ran the full suite a second time end-to-end — 391/391 clean.
  No code changed between any of these runs (only `live_state.json`'s
  data grew via the evolve batch, which had already fully completed —
  confirmed no stray `evolve`/`pytest` processes — before the first pytest
  invocation started). Treating this as a non-reproducible flake in the
  full-suite run rather than a real defect; nothing to fix. Worth watching
  for a repeat, since neither `AGENTS.md`'s history nor this session's own
  investigation found a mechanism (no `pytest-xdist`, no background process
  found still touching the real file).
- Direct top-level key diff of `live_state.json` (working tree vs. committed
  `HEAD`): only `updated`/`researcher_memory`/`lineage` changed; `genome`
  (still v3) and `broker` byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged (`evotrader_bundle.py
  summary`'s own check).
- `tools/edit_bundle_module.py verify`/`sync --check` both clean.
- Dashboard rebuilt (`index.html`).

Genome still v3 (1d) live, untouched. No trading, no constitution change.

# 3-hourly check: evolve batch v3 (gen 13548->13755)

**When:** 2026-09-13, ~03:46-04:13 UTC
**What:** 15 more real `evolve` generations against the live v3 (1d) champion.
**Result:** no promotion. Cumulative candidates tried against v3 rose
13548 -> 13755, boldness/stagnation counter 972 -> 987.

## Context / freshness checks before running

- Repo cloned in detached HEAD, `main` up to date with `origin/main` already
  (no divergence — a straight `git checkout main` left the branch pointer
  matching `origin/main`'s tip, `git pull origin main` reported "Already
  up to date" content-wise, just moved off detached HEAD).
- `live_state.json`'s `updated` was `2026-09-13T01:17:13+00:00` (from the
  ~01:20 UTC evolve batch), `runs/2026-09-13-0020-daily-trading.md` confirms
  today's daily bar (tick 30) was already handled at 00:20 UTC. No new bar to
  process — did not run `tick`.
- `review-hard-calls`: still 0 pending (1 reviewed so far, unchanged). Item 4
  (LLM-backed consults) still blocked on a real hard-call flag.
- `holdout-pressure`: 275 draws going in.
- Items 2/5/6 still not a scheduled session's call (owner-gated). Items
  3/7/8/9/10 resolved/feature-complete. So the cycle ran the standing evolve
  batch, same as recent sessions.

## Run

`tools/background_runner.py start` + backgrounded `wait` for
`python3 evotrader_bundle.py evolve 15`, ~26.8-minute real run (1610s),
exit code 0, no truncation.

Champion's fold-aggregate fitness held flat at 1.057 across all 15
generations (965 trades, win 39%, stops 1%, halts 3 — unchanged).

**Notably lively batch, again**: raw best-of-generation fold-fitness beat the
champion's own 1.057 in **15/15 generations** (range 1.208-2.103) — every
single generation cleared the raw-fitness bar, none tied or lost. This is
even livelier than the prior batch's already-flagged 14/15 (see
`runs/2026-09-13-0120-evolve-batch-v3.md`), continuing the trend that batch's
note asked future sessions to watch. Still no promotion: every candidate that
cleared the fold-aggregate gate lost the sealed holdout on its own draw.
`holdout-pressure` draw count rose 275 -> 284 (+9 new losing draws), margin
unchanged in shape (~6.70-6.72).

Two consecutive livelier-than-usual batches (14/15 then 15/15 clearing raw
fitness) is worth flagging explicitly rather than reading in isolation, same
caveat the prior note raised — not enough on its own to indicate anything
about the champion's health or a change in the search's behavior, since the
sliding 4-year fold-aggregate window and the champion's own fold-aggregate
fitness (1.057, well down from two batches ago's 1.508) move together. If a
third consecutive batch shows the same pattern, that's the point a future
session should look closer (e.g. `fold-date-sensitivity` or a re-check of
whether the window shift itself explains it) rather than just noting it
again.

## Verification before commit

- `python3 -m pytest -q`: 391/391 both before (baseline) and after `evolve`.
- Direct top-level key diff of `live_state.json`: only
  `lineage`/`researcher_memory`/`updated` changed; `genome`, `broker`,
  `journal`, `ticks`, `hard_call_reviews` all byte-identical (Python
  equality check, not just eyeballing).
- Constitution: `evotrader_bundle.py summary` prints `constitution verified
  726dfa4bac85891a` (unchanged); `evotrader.manifest` md5 also checked.
- `tools/edit_bundle_module.py verify` and `sync --check` both clean.
- Dashboard rebuilt (`index.html`).

Genome still v3 (1d) live, untouched.

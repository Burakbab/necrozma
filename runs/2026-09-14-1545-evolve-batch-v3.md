# Evolve batch — 2026-09-14 ~09:47-10:12 UTC (3-hourly check, delayed write-up)

## Session start

Repo was in detached HEAD, up to date with `origin/main` at `22049c3`.
`git checkout main` + `git pull origin main` fast-forwarded cleanly (33
commits), no divergence. `pip3 install -r requirements.txt -q` (cloud
sandbox starts bare). `python3 -m pytest -q` 406/406 baseline before any
work.

## No live trading this cycle

Tick 31 already handled at 00:20 UTC. `live_state.json`'s `updated` was
`2026-09-14T07:18:04+00:00` (from the prior ~06:47-07:21 UTC evolve batch),
confirmed before starting — no new bar to process.

## Freshness checks before picking work

- `review-hard-calls`: 0 pending (1 reviewed so far, unchanged) — item 4
  still not actionable.
- Items 2/5/6 (4h-bar shadow evolution, short-selling Phase 2 wiring,
  equities/FX data source): still not a scheduled session's call.
- Items 3/7/8/9/10: resolved/feature-complete, nothing new to pick up.

With nothing else actionable, ran the standing `evolve` batch against the
live v3 (1d) champion, matching the established 3-hourly cadence.

## What ran

`tools/background_runner.py start`/`wait` (no `nohup`/`&`, per item 9's
mechanical fix) — `python3 evotrader_bundle.py evolve 15`, exit code 0, no
truncation (log captured from the first byte).

- Champion v3's fold-aggregate fitness held flat at **0.973** across all 15
  generations (965 trades, 39% win rate, 1% stop rate, 3 halts — unchanged
  throughout).
- Raw best-of-generation fold-fitness beat the champion's own 0.973 in
  **15/15 generations** (range 1.113-2.225).
- No proposal ever cleared the promotion bar — champion held every
  generation.
- Cumulative candidates tried against v3 rose 15630 -> 15838; boldness/
  stagnation counter rose 1123 -> 1137.

## Container restart mid-cycle

The container running this session was restarted while waiting on the
evolve batch and a subsequent `pytest` verification run. The evolve batch
itself had already completed and written its results to `live_state.json`
before the restart, so no work was lost — but a large gap (~5.5 hours)
opened between when the batch actually ran (~10:12 UTC) and when this note
and commit were finally written (~15:45 UTC). During that gap another
scheduled session landed `2296c4a` ("Wire the discretionary 'cover' intent
for closing an open short", item 5 Phase 2) on `origin/main`. That commit's
own message states `live_state.json` and the constitution were untouched,
confirmed here by diffing `live_state.json` across `df300cc` (this
session's own prior small commit) and `2296c4a` (identical). Resolved by
`git stash` (this session's uncommitted `live_state.json`/`index.html`
evolve-batch output) -> `git pull --ff-only origin main` -> `git stash
pop`: clean merge, no conflicts, since the two changesets touched disjoint
files. Re-verified `python3 -m pytest -q` 415/415 (406 baseline + 9 new
from the cover-intent commit) after the merge, plus
`review-hard-calls`/`edit_bundle_module.py verify`/`sync --check` all clean
against the merged tree, before rebuilding the dashboard and committing.

## Verification

- `python3 -m pytest -q`: 406/406 baseline (pre-evolve), 415/415 after
  merging in the concurrent cover-intent commit (406 + 9 new).
- Direct top-level key diff of `live_state.json` (before vs. after the
  evolve batch): only `lineage`/`researcher_memory`/`updated` changed;
  `genome`/`broker`/`journal`/`hard_call_reviews` byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged throughout.
- `tools/edit_bundle_module.py verify`/`sync --check` both clean.
- Dashboard rebuilt (`index.html`) against the final merged tree.

Genome still v3 (1d) live, untouched. No promotion.

# Evolve batch — 2026-09-14 ~15:47-16:22 UTC (3-hourly check)

## Session start

Repo started up to date with `origin/main` (fast-forwarded cleanly from
`22049c3` to `d773d01`, no divergence). `pip3 install -r requirements.txt -q`
(cloud sandbox starts bare). `python3 -m pytest -q` 415/415 baseline before
any work.

## No live trading this cycle

Tick 31 already handled at 00:20 UTC. `live_state.json`'s `updated` was
`2026-09-14T10:12:46+00:00` (from the prior ~09:47-10:12 UTC evolve batch),
confirmed before starting — no new bar to process.

## Freshness checks before picking work

- `review-hard-calls`: 0 pending (1 reviewed so far, unchanged) — item 4
  still not actionable.
- Items 2/5/6 (4h-bar shadow evolution, short-selling Phase 2's "should a
  consult be allowed to open a short" owner decision, equities/FX data
  source): still not a scheduled session's call.
- Items 3/7/8/9/10: resolved/feature-complete, nothing new to pick up.

With nothing else actionable, ran the standing `evolve` batch against the
live v3 (1d) champion, matching the established 3-hourly cadence.

## What ran

`tools/background_runner.py start`/`wait` (no `nohup`/`&`, per item 9's
mechanical fix) — `python3 evotrader_bundle.py evolve 15`, exit code 0, no
truncation.

- Champion v3's fold-aggregate fitness held flat at **0.973** across all 15
  generations (965 trades, 39% win rate, 1% stop rate, 3 halts — unchanged
  throughout).
- Raw best-of-generation fold-fitness beat the champion's own 0.973 in
  **15/15 generations** (range 1.165-2.077).
- No proposal ever cleared the promotion bar — champion held every
  generation.
- Cumulative candidates tried against v3 rose 15838 -> 16048; boldness/
  stagnation counter rose 1137 -> 1152.

## Network-flake test investigation (not evolve-batch-caused)

Two separate full `python3 -m pytest -q` runs after the evolve batch each
showed exactly **1 failure** out of 415 (414/415), but a *different* test
failed each time: `test_evolve_dry_run_resumes_researcher_memory` once,
`test_evolve_saves_and_matches_its_own_dry_run_decision` once, both in
`tests/test_run_from_files_matches_bundle.py`. Both failures trace to the
same root cause: a live HTTP fetch inside the test to
`https://data-api.binance.vision/api/v3/klines` for a deliberately-fake test
symbol `ZZTESTAUSDT`, returning `HTTP Error 400: Bad Request` through this
container's outbound proxy — a live external network call inside a unit
test, not anything touched by this session's evolve batch or genome state.
A targeted re-run of just that file, `python3 -m pytest -q
tests/test_run_from_files_matches_bundle.py`, passed clean **13/13**,
confirming it's a transient network/proxy flake rather than a real
regression: the pre-evolve baseline this session ran was clean 415/415, and
`live_state.json`'s diff (below) shows the evolve batch touched nothing this
test's logic depends on. Not investigated further or fixed — flagging here
in case it recurs often enough for a future session to consider retrying
that specific outbound call or mocking the test symbol's fetch.

## Verification

- `python3 -m pytest -q`: 415/415 baseline (pre-evolve); two post-evolve
  full runs each showed 414/415 with the network-flake test above (a
  different one each time); a third, targeted run of just the affected test
  file passed clean 13/13, confirming the flake and ruling out a real
  regression.
- Direct top-level key diff of `live_state.json` (before vs. after the
  evolve batch): only `lineage`/`researcher_memory`/`updated` changed;
  `genome`/`broker`/`journal`/`hard_call_reviews` byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged throughout
  (`review-hard-calls`, 0 pending).
- `tools/edit_bundle_module.py verify`/`sync --check` both clean (no source
  files touched this cycle).
- Dashboard rebuilt (`index.html`) against the updated `live_state.json`.

Genome still v3 (1d) live, untouched. No promotion.

# 3-hourly check: evolve batch v3 (gen 17724->17920), no promotion

2026-09-16, ~03:49-04:14 UTC.

## Freshness checks before starting

- `live_state.json` `updated` was `2026-09-16T01:00:04+00:00`, from the prior
  ~00:48-01:04 UTC item-12 run (hash-based `researcher_memory["tested"]`
  identity fix, see `runs/2026-09-16-0104-item12-tested-memory-hash-fix.md`).
  Today's daily bar (tick 33) was already handled at 00:20 UTC
  (`runs/2026-09-16-0020-daily-trading.md`) — confirmed via `summary`
  before touching anything, no live trading this cycle.
- `review-hard-calls`: 0 pending, 2 reviewed (unchanged) — item 4 has
  nothing new to act on.
- Owner-decision items 2/5/6 still not a scheduled session's call. Item 7
  (unflatten) is explicitly optional/last. Items 8/9/10/11/12 all
  resolved. Item 0 (fitness-vs-excess-return) closed, no revisit trigger
  fired. So the cycle ran the standing evolve batch.

## What ran

`python3 tools/background_runner.py start` (`evolve 15`) + backgrounded
`wait`, exit code 0, no truncation — 15 real generations against the live
v3 (1d) champion. Cumulative candidates tried against v3 rose
17724 → 17920 (matches 15 gens × 14 proposals/gen = 210), stagnation
counter 1273 → 1288.

Champion's fold-aggregate fitness held flat at 1.635 across all 15
generations (965 trades, 39% win, 1% stops, 4 halts — unchanged
throughout). Raw best-of-generation fold-fitness beat the champion's own
1.635 in **8/15 generations** (53%: gens 5, 7, 8, 10, 11, 12, 13, 14;
range across all 15 generations 1.266-2.080). No proposal cleared the
promotion bar in any generation — champion holds.

## Verification

- `python3 -m pytest -q`: 422/422 both before (baseline) and after the
  evolve batch — no flakes this cycle.
- Direct top-level key diff of `live_state.json` (committed HEAD vs.
  working copy): only `lineage`, `researcher_memory`, `updated` changed —
  `genome`, `broker`, `journal`, `hard_call_reviews` byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged.
- `tools/edit_bundle_module.py sync --check` / `verify`: both clean, no
  drift.
- Dashboard rebuilt (`index.html`).

Genome still v3 (1d) live, untouched. No genome/champion promotion this
cycle.

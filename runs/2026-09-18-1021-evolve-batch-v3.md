# 3-hourly check: evolve batch v3 (gen 21276->21483), no promotion

**When**: 2026-09-18, ~09:48-10:21 UTC

## Context

Daily bar already handled at 00:20 UTC (`runs/2026-09-18-0020-daily-trading.md`,
`live_state.json` `updated` was `2026-09-18T07:19:41+00:00` from the prior
3-hourly check's own evolve batch before this run started) — no live trading
this cycle. Freshness checks before running: `review-hard-calls` still 0
pending (2 reviewed, unchanged), items 2/5/6 still not a scheduled session's
call, item 7 feature-complete, item 4 blocked on a real hard-call flag (none
pending). With nothing else queued, used the slot for one more real
15-generation batch (offline/shadow development against the live champion).

## What ran

`pip3 install --user -r requirements.txt -q --timeout 60 --retries 5` (plain
`pip3 install -r requirements.txt -q` timed out twice against
`files.pythonhosted.org`; the `--user` + longer timeout + retries combination
worked cleanly, consistent with prior sessions' note that this is a recurring
step-1.5 flake with more than one workaround shape).

`python3 evotrader_bundle.py evolve 15` via `tools/background_runner.py`
(`start` + backgrounded `wait`), exit code 0, no truncation (78-line log,
all 15 generations present).

## Result

Champion's fold-aggregate fitness held flat at **1.858** across all 15
generations (970 trades, 41% win, 1% stops, 3 halts, unchanged throughout).
No proposal cleared the promotion bar in any generation — champion holds.

Raw best-of-generation fold-fitness beat or tied the champion's own 1.858 in
**5/15 generations** (33%: gen 2 = 1.935, gen 5 = 1.966, gen 12 = 1.860,
gen 14 = 1.859 — all strict beats — plus gen 13's exact tie at 1.858).

Cumulative candidates tried against v3 rose 21276 → 21483, stagnation/boldness
counter 1530 → 1545.

## Verification before commit

- `python3 -m pytest -q`: 422/422 both before (baseline) and after `evolve`.
- Direct top-level key diff of `live_state.json` (before vs. after): only
  `lineage`, `researcher_memory`, `updated` changed — genome, broker, journal,
  hard_call_reviews byte-identical.
- Constitution verified `726dfa4bac85891a` unchanged (`evolve` and `summary`
  both print the same hash on load).
- `tools/edit_bundle_module.py verify`: "round-trip verified: bundle
  unchanged".
- `tools/edit_bundle_module.py sync --check`: "bundle already matches real
  files, no changes".
- Dashboard rebuilt (`index.html`).

Genome still v3 (1d) live, untouched.

## Git

Container started in detached HEAD with local `main` at `1c4fbff`, matching
`origin/main`'s tip exactly (`git fetch` reported a "forced update" but
`git log` on both refs showed identical history at that commit — no actual
divergence this cycle). `git checkout main && git reset --hard origin/main`
re-pointed the local branch cleanly, no content lost.

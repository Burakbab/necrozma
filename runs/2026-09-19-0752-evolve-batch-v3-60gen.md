# Weekend deep evolve batch — 2026-09-19 (~06:20-07:45 UTC)

60 real `evolve` generations against the live v3 (1d) champion, no promotion —
deeper than a routine 3-hourly batch (15 gens), using this session's larger
time budget. Cumulative candidates tried against v3 rose 22523 → 23354 (+831),
stagnation/boldness counter 1620 → 1680.

Champion's fold-aggregate fitness held flat at 1.465 across all 60 generations
(943 trades, 38% win, 1% stops, 4 halts, unchanged throughout). Best-of-generation
fold-fitness ranged roughly 1.293-2.554 across the run, regularly beating the
champion's own 1.465 by a comfortable margin, but never by enough to clear the
cumulative multiple-testing bar (now built on 23k+ prior draws against this
champion).

Run via `tools/background_runner.py` (`start` + backgrounded `wait`), ~85-minute
real run, exit code 0, no truncation. Verified before commit: `python3 -m
pytest -q` 422/422 both before (baseline) and after `evolve`; direct top-level
key diff of `live_state.json` showed only `lineage`/`researcher_memory`/
`updated` changed (genome, broker, journal, hard_call_reviews byte-identical);
constitution manifest `726dfa4bac85891a` unchanged; `tools/edit_bundle_module.py
verify`/`sync --check` both clean; dashboard rebuilt with `EVO_STATE` set.
Genome still v3 (1d) live, untouched.

Freshness checks before starting: `succession-audit` and `holdout-pressure`
re-run fresh (see the main weekend write-up for both) — no change from the
picture already recorded in AGENTS.md (neither v1 nor v2 clears the
dd-corrected gate either; holdout-pressure margin at 7.048/497 draws going in).
No live trading this session (tick 36 already handled at 00:20 UTC).

See `runs/2026-09-19-0600-weekend-all-hands.md` for the full session write-up,
including the seed-convergence experiment this batch's slot was paired with.

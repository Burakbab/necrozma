# Evolve batch — 2026-09-21 01:22 UTC

## Summary

- After the hard-call review work (see
  `runs/2026-09-21-0051-first-real-hard-call-review.md`), with nothing else
  queued this cycle (freshness checks: `review-hard-calls` 0 pending after
  that review, 3 reviewed; items 2/5/6/13 still not a scheduled session's
  call; `AGENTS.md` size 228,180 bytes at commit, well under the 256KB
  rotation threshold), used the rest of the slot for one more real
  15-generation `evolve` batch (offline/shadow development against the
  live v3 champion) via `tools/background_runner.py` (`start` +
  backgrounded `wait`), exit code 0, no truncation, ~30 minutes.
- Champion's fitness held flat at 1.636 across all 15 generations (943
  trades, 38% win, 1% stops, 4 halts, unchanged throughout). Best-of
  generation fitness ranged 1.469-2.413, never clearing the
  promotion-margin bar.
- Cumulative candidates tried against v3 rose 25854 → 26059, stagnation/
  boldness counter 1860 → 1875.
- Note: the reported champion fitness (1.636, 943 trades) differs from the
  ~1.728/960-trade figure quoted in recent prior cycles' entries — this
  tracks the rolling backtest window advancing by one more daily bar
  (tick 38 landed since the last evolve batch), not a champion change;
  genome version and all trading-relevant state are byte-identical
  before/after.

## Verification

- `python3 -m pytest -q` → 426/426 passed after the batch.
- Top-level key diff of `live_state.json`: only `researcher_memory`,
  `lineage`, `updated` changed; `genome`, `broker`, `journal`,
  `hard_call_reviews` byte-identical.
- `tools/edit_bundle_module.py verify` → round-trip verified, bundle
  unchanged. `sync --check` → bundle already matches real files.
- `holdout-pressure` re-checked (read-only, no state change) — margin rose
  slightly (7.082, was 7.078) at draw 528, nothing new.
- Dashboard rebuilt via `EVO_STATE="$(pwd)/live_state.json" python3
  evotrader_dashboard.py` — wrote `index.html` (19.8 KB), no errors.
- Genome still v3 (1d) live, untouched. No promotion this cycle, so no
  README.md `## Status` update needed.

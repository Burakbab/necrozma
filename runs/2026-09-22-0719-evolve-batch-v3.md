# 2026-09-22 ~06:47-07:19 UTC — 3-hourly check: evolve batch vs live v3 champion

## Freshness checks (before running anything)

- `live_state.json`'s `updated` was `2026-09-22T04:18:07+00:00` at session
  start, matching the prior 3-hourly check's own evolve batch
  (`runs/2026-09-22-0421-evolve-batch-v3.md`), not a new tick. Today's daily
  bar was already handled at the dedicated 00:20 UTC slot
  (`runs/2026-09-22-0020-daily-trading.md`, NAV $14,227.20, held, no trade).
  No live trading this cycle.
- `review-hard-calls`: 0 pending, 3 reviewed — unchanged.
- Owner-decision items (6, 13) still open, not a scheduled session's call.
  Items 2/5/9/10/12 resolved/parked. Item 7 (bundle unflatten) explicitly
  optional/last. Item 4's infra is complete (2 real hard-call reviews
  recorded, 0 pending).
- `AGENTS.md` size ~250.7KB — under the 256KB single-read threshold, no
  archival needed this cycle (close to it — a future session should watch
  this).
- With nothing else queued, used the slot for one more real 15-generation
  `evolve` batch against the live champion, per established pattern.
- Container arrived in detached HEAD, 47 commits behind `origin/main` —
  `git checkout main && git pull origin main` fast-forwarded cleanly,
  nothing lost.

## What ran

`python3 -m pytest -q` baseline: 426/426 passed (155.93s), before touching
anything.

`python3 tools/background_runner.py start --log /tmp/evolve_run/evolve.log
--status /tmp/evolve_run/evolve.status -- python3 evotrader_bundle.py evolve
15`, then `wait --timeout 2400`. Exit code 0, ~29 minutes, no truncation.

Champion's fold-aggregate fitness held flat at **1.573** across all 15
generations (947 trades, 38% win, 1% stops, 4 halts, unchanged throughout).
Best-of-generation fold-fitness ranged 1.564–1.921, never clearing the
promotion-margin bar. No promotion — `[evolve] champion held`.

## Verification before commit

- Cumulative candidates tried against v3: **27930 → 28137**
  (`researcher_memory.tested` length, confirmed directly from
  `live_state.json`, not just the log). Stagnation/boldness counter:
  **2011 → 2026**.
- Top-level key diff of `live_state.json`: only `researcher_memory`,
  `lineage`, `updated` changed. `genome` (still v3), `broker`, `journal`,
  `hard_call_reviews` byte-identical (`before == after` checked directly in
  Python, not just eyeballed). `lineage` length unchanged at 202 (bounded
  ring buffer, no new promotion attempt recorded).
- `python3 -m pytest -q` after the batch: 426/426 passed (154.38s) — same
  count as baseline, no flake.
- `tools/edit_bundle_module.py verify`: bundle unchanged (round-trip
  verified). `sync --check`: bundle already matches real files.
- `holdout-pressure` re-checked (read-only, no state change): margin rose
  slightly to 7.109 (was 7.108) at draw 554 — same slow-rise pattern already
  tracked under item 13, nothing new.
- Dashboard rebuilt with `EVO_STATE` set — `index.html` now shows 28137
  challenger ideas tried.

Genome still v3 (1d) live, untouched. No constitution change (manifest
unchanged).

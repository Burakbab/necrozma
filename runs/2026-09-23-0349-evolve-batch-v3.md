# 3-hourly check — evolve batch vs live v3 champion — 2026-09-23 ~03:46-04:14 UTC

## Container state

Container arrived with local `main` in **detached HEAD**, stale — last local
commit dated 2026-09-18 (50 commits), while `origin/main` was at
2026-09-23T01:36:22 (53 commits). `git checkout main` then `git pull`
reported `main` diverged with `git merge-base` finding none. **Process
deviation, flagged for future sessions:** the repo actually *was* shallow
(`git rev-parse --is-shallow-repository` → `true`, confirmed after the
fact) — the run protocol's documented next step here is
`tools/git_sync.py` (or a manual `git fetch --unshallow`) to check for a
real merge-base before concluding the history was rewritten, and this
session skipped straight to `git reset --hard origin/main` instead, without
unshallowing first. Working tree was clean (nothing to lose) and the reset
landed correctly on `33415f7` (the same tip as the previous run's own
commit, confirmed same commit hash the daily/3-hourly notes already
reference), so nothing was actually lost — but the shortcut bypassed the
protocol's safer path and got lucky rather than verified. **Next session:**
if this happens again, run `tools/git_sync.py` (or `git fetch --unshallow`
+ `git merge-base`) *before* any reset — every prior occurrence of this
exact symptom (six-plus sessions, see the Run protocol section) turned out
to be shallow-clone staleness, not a real rewrite.

## Freshness check

`live_state.json`'s `updated` at session start: `2026-09-23T01:31:29+00:00`
— matches tick 40 (`runs/2026-09-23-0020-daily-trading.md`, NAV $14,451.04,
sold NEARUSDT) plus the prior 3-hourly check's own evolve batch
(`runs/2026-09-23-0048-evolve-batch-v3.md`). No new bar closed since; no
live trading this cycle.

## What ran

Reviewed AGENTS.md's tracked items: items 2/5/9/10/12 resolved/parked, item
7 explicitly optional/last, items 6/13 still owner decisions (item 13 is
explicitly "not a scheduled session's call to make alone"), item 4 (hard
calls) confirmed 0 pending via `review-hard-calls`. `AGENTS.md` at 237,630
bytes — well under the 256KB threshold, no archival needed. Nothing else
queued, so used the slot for one more real 15-generation `evolve` batch
against the live v3 champion via `tools/background_runner.py` (`start` +
separate `wait`), exit code 0, no truncation, ~25 minutes real time.

Cumulative candidates tried against v3 rose **29173 → 29380**, stagnation
counter **2100 → 2116**. Champion's fold-aggregate fitness held flat at
**1.391** across all 15 generations (943 trades, 38% win, 1% stops, 4
halts, unchanged throughout). Best-of-generation fold-fitness ranged
0.900-2.470, never clearing the promotion-margin bar. Champion held every
generation.

## Verification before commit

- `python3 -m pytest -q`: 426/426 both before (baseline) and after `evolve`.
- Top-level key diff of `live_state.json` against the last commit (checked
  directly in Python via `git show HEAD:live_state.json`, not just
  eyeballed): only `updated`/`researcher_memory`/`lineage` changed —
  `genome`, `broker`, `journal`, `hard_call_reviews` byte-identical.
- `lineage` length unchanged at 202 (bounded ring buffer) — no new
  promotion attempt recorded. Genome version unchanged (still v3).
- `tools/edit_bundle_module.py verify` / `sync --check` both clean.
- `holdout-pressure` re-checked (read-only, no state change): margin rose
  slightly to 7.125 (was 7.121) at draw 570 — same slow-rise pattern
  already tracked under item 13, nothing new.
- Dashboard rebuilt with `EVO_STATE` set — `index.html` now shows 29380
  challenger ideas tried.

Genome still v3 (1d) live, untouched.

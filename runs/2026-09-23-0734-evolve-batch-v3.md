# 3-hourly check — evolve batch vs live v3 champion — 2026-09-23 ~06:47-07:34 UTC

## Container state

Container arrived shallow-cloned in detached HEAD with local `main` stale
(`aaee4c8`, 50 commits behind `origin/main`'s `8b1b7ae`, 52 ahead). Ran
`tools/git_sync.py` per the run protocol (previous session's own flagged
process note) instead of jumping straight to a hard reset — it unshallowed,
found a real merge-base, and fast-forwarded cleanly (`aaee4c8..8b1b7ae`,
nothing discarded). Confirms this was shallow-clone staleness again, not a
real rewrite, matching every prior occurrence of this symptom.

## Freshness check

`live_state.json`'s `updated` at session start: `2026-09-23T04:11:39+00:00`
— matches the prior 3-hourly check's own evolve batch
(`runs/2026-09-23-0349-evolve-batch-v3.md`). Daily bar already handled at
the dedicated 00:20 UTC slot (tick 40); no new bar closed since. No live
trading this cycle.

## What ran

Reviewed AGENTS.md's tracked items: items 2/5/9/10/12 resolved/parked, item
7 explicitly optional/last, items 6/13 still owner decisions (item 13 is
explicitly "not a scheduled session's call to make alone"), item 4 (hard
calls) confirmed 0 pending via `review-hard-calls`. `AGENTS.md` at 240,816
bytes — well under the 256KB threshold, no archival needed. Nothing else
queued, so used the slot for one more real 15-generation `evolve` batch
against the live v3 champion via `tools/background_runner.py` (`start` +
separate `wait`), exit code 0, no truncation, ~38 minutes real time.

Cumulative candidates tried against v3 rose **29380 → 29587**, stagnation
counter **2116 → 2130**. Champion's fold-aggregate fitness held flat at
**1.391** across all 15 generations (943 trades, 38% win, 1% stops, 4
halts, unchanged throughout). Best-of-generation fold-fitness ranged
1.454-2.243, never clearing the promotion-margin bar. Champion held every
generation.

## Verification before commit

- `python3 -m pytest -q`: 426/426 both before (baseline) and after `evolve`.
- Top-level key diff of `live_state.json` against the last commit (checked
  directly in Python via `git show HEAD:live_state.json`, not just
  eyeballed): only `updated`/`researcher_memory`/`lineage` changed —
  `genome`, `broker`, `journal`, `hard_call_reviews` byte-identical.
- `lineage` length unchanged at 202 (bounded ring buffer) — no new
  promotion attempt recorded. Genome version unchanged (still v3).
- `tools/edit_bundle_module.py verify` / `sync --check` both clean.
- `holdout-pressure` re-checked (read-only, no state change): margin rose
  slightly to 7.133 (was 7.121 two cycles ago) at draw 578 — same slow-rise
  pattern already tracked under item 13, nothing new.
- Dashboard rebuilt with `EVO_STATE` set — `index.html` now shows 29587
  challenger ideas tried.

Genome still v3 (1d) live, untouched.

# 3-hourly check: 15 more evolve generations against v3 champion, no promotion

2026-09-25, ~12:47-13:16 UTC.

## Context

No live trading this cycle: tick 42 was already handled at the dedicated
00:20 UTC daily slot, and the prior 3-hourly check's own evolve batch had
already run at ~09:53-10:18 UTC (confirmed via `live_state.json`'s `updated`
timestamp at session start, `2026-09-25T10:12:26+00:00`, matching
`runs/2026-09-25-1018-evolve-batch-v3.md`).

Freshness checks before running:
- `review-hard-calls`: 0 pending (4 reviewed, unchanged).
- Items 6/13 under "Owner decisions pending": still genuine owner calls, not
  actionable by a scheduled session.
- `AGENTS.md` size: 250,688 bytes at session start — under the 256KB
  single-read threshold.

With nothing else queued, used the slot for one more real 15-generation
`evolve` batch (development against the live champion) via
`tools/background_runner.py` (`start` + separate `wait`), exit code 0, no
truncation, ~24 minutes.

Container arrived detached from any branch, `git checkout main` reported 50
local vs. 50 origin commits diverged — the recurring shallow-clone staleness
pattern this file already documents repeatedly, not a real rewrite.
`tools/git_sync.py` fast-forwarded cleanly (`aaee4c8..c4459ff`), nothing
lost.

## Result

Champion v3's fold-aggregate fitness held flat at **1.776** across all 15
generations (969 trades, 39% win, 1% stops, 3 halts, unchanged throughout).
Best-of-generation fold-fitness ranged 0.719-2.127, never clearing the
promotion-margin bar. Cumulative candidates tried against v3 rose
32936 → 33142 (per `researcher_memory.tested`), stagnation/boldness counter
2374 → 2388.

## Verification before commit

- `python3 -m pytest -q`: 426/426 passed both before (baseline, 146.45s)
  and after (145.12s) the batch, no flakes.
- Top-level key diff of `live_state.json` (checked directly in Python
  against `git show HEAD:live_state.json`, not just eyeballed): only
  `updated`/`researcher_memory`/`lineage` changed. Genome, broker, journal,
  hard_call_reviews byte-identical.
- `lineage` length unchanged at 202 (bounded ring buffer) — no new
  promotion attempt recorded.
- `tools/edit_bundle_module.py verify` / `sync --check`: both clean.
- `holdout-pressure` re-checked (read-only, no state change): margin
  unchanged at 7.192, draw 643 — same as before the batch (expected, since
  no candidate cleared the fold-aggregate gate this cycle, so no new
  holdout draw happened). Same slow-rise pattern already tracked under
  item 13, nothing new.
- Dashboard rebuilt with `EVO_STATE` set; `index.html` shows 33142
  challenger ideas tried.

Genome still v3 (1d) live, untouched.
